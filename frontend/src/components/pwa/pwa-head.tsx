'use client';

/**
 * PWA Head Component
 *
 * Renders additional PWA-related meta tags and links in the document head.
 * This complements the static metadata in layout.tsx with dynamic/client-side elements.
 */
export function PWAHead() {
  // This component is currently a placeholder
  // Most PWA meta tags are handled in layout.tsx metadata
  // This can be used for dynamic meta tag updates in the future
  return null;
}
